name: Run Local.ch Automation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  run-selenium:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Browsers (Edge + Chrome)
        run: |
          sudo apt update
          sudo apt install -y wget curl unzip gnupg software-properties-common

          # --- Install Microsoft Edge ---
          wget https://packages.microsoft.com/keys/microsoft.asc -O- | sudo apt-key add -
          sudo add-apt-repository "deb [arch=amd64] https://packages.microsoft.com/repos/edge stable main"
          sudo apt update
          sudo apt install -y microsoft-edge-stable || echo "⚠️ Edge install skipped"

          # --- Install Google Chrome (as fallback) ---
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo apt install -y ./google-chrome-stable_current_amd64.deb || echo "⚠️ Chrome install skipped"

      - name: Install Edge WebDriver (robust fallback)
        run: |
          set -e
          echo "🧩 Checking Microsoft Edge version..."
          EDGE_VERSION=$(microsoft-edge-stable --version | awk '{print $3}' || echo "")
          
          if [ -z "$EDGE_VERSION" ]; then
            echo "⚠️ Edge not found. Skipping to Chrome setup."
            exit 0
          fi

          MAJOR_VERSION=$(echo "$EDGE_VERSION" | cut -d'.' -f1)
          echo "Detected Edge version: $EDGE_VERSION (major $MAJOR_VERSION)"

          DRIVER_VERSION=$(curl -s "https://msedgedriver.azureedge.net/LATEST_RELEASE_${MAJOR_VERSION}" | tr -d '\r\n')

          if [ -z "$DRIVER_VERSION" ]; then
            echo "⚠️ Could not fetch version-specific driver, using latest stable."
            DRIVER_VERSION=$(curl -s "https://msedgedriver.azureedge.net/LATEST_RELEASE" | tr -d '\r\n')
          fi

          echo "Resolved EdgeDriver version: $DRIVER_VERSION"
          DRIVER_URL="https://msedgedriver.azureedge.net/${DRIVER_VERSION}/edgedriver_linux64.zip"
          echo "Downloading EdgeDriver from: $DRIVER_URL"

          wget -q "$DRIVER_URL" -O msedgedriver.zip || (echo "❌ EdgeDriver download failed, fallback to ChromeDriver"; exit 0)
          unzip -q msedgedriver.zip
          sudo mv msedgedriver /usr/local/bin/
          chmod +x /usr/local/bin/msedgedriver
          msedgedriver --version || echo "⚠️ EdgeDriver check skipped"

      - name: Install ChromeDriver (only if Edge failed)
        run: |
          if ! command -v msedgedriver &> /dev/null
          then
            echo "🚨 EdgeDriver not found — installing ChromeDriver as fallback"
            CHROME_VERSION=$(google-chrome --version | awk '{print $3}' | cut -d'.' -f1)
            DRIVER_VERSION=$(curl -s "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROME_VERSION}" || echo "")
            if [ -z "$DRIVER_VERSION" ]; then
              DRIVER_VERSION=$(curl -s "https://chromedriver.storage.googleapis.com/LATEST_RELEASE")
            fi
            wget -q "https://chromedriver.storage.googleapis.com/${DRIVER_VERSION}/chromedriver_linux64.zip" -O chromedriver.zip
            unzip -q chromedriver.zip
            sudo mv chromedriver /usr/local/bin/
            chmod +x /usr/local/bin/chromedriver
            chromedriver --version
          fi

      - name: Run Automation Script
        env:
          MAILTRAP_USER: ${{ secrets.MAILTRAP_USER }}
          MAILTRAP_PASS: ${{ secrets.MAILTRAP_PASS }}
        run: |
          echo "🚀 Starting automation"
          python juste_scraping.py
